package test;

/**
 * Created by marcus.rodrigues on 29/06/2015.
 */
public class Comentados {

    /*
        //Abre arquivo .cvs
        FileReader arquicoCSV = null;

        arquicoCSV = new FileReader("C:\\Users\\marcus.rodrigues\\Desktop\\grafo.txt");

        BufferedReader myBuffer = new BufferedReader(arquicoCSV);
        Vertice vertice;

        //Le linhas do arquivo
        String linha = null;

        linha = myBuffer.readLine();

        for (int i = 0; i < rotas.length; i++){
            rotas[i] =linha;
            //criaObjetosDeItem(linha, estoque, livroList);
            linha = myBuffer.readLine();//Não sei por que isso esta funcionando mas esta, então deixa assim
            if(i < 13629){
                //vertices
                //vertices[i] = criaVertice(linha);
                grafo.adicionarVertice(criaVertice(linha));

            }else if(i > 13629){
                //arestas
                grafo.

            }


            System.out.println(i + " - " + linha);
        }
        arquicoCSV.close();
        */
}
